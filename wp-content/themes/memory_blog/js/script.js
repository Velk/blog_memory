/*
const elements = document.querySelectorAll('.data li')
const select = document.querySelector('#select_theme')

select.addEventListener('change', () => {
    elements.forEach( elem => {
        const attr = elem.getAttribute('data-style');

        if (attr === select.value || select.value === "*") {
            elem.classList.remove('disable')
        } else {
            elem.classList.add('disable')
        }
    })
})
*/
